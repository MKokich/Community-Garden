     <link href= "./css/adopt.css" rel="stylesheet">
    </head>

<body>

<section class="container">
  <h3 class="gallery-name">Adopt!!!</h3>
  <section class="flex-row justify-center">
<!-- For each gallery, the gallery-details partial is used to render the gallery data -->
    {{#each post}}
    <p>{{> displayAdopt}}</p>
    {{/each}}
  </section>
</section>

<script src="/path/to/handlebars.min.js"></script>