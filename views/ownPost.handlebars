<nav class="navbar navbar-expand-lg navbar-light bg-light">

  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item">
       <a class="btn btn-outline" onclick="window.location" aria-current="page" href="/home">home</a>
      </li>
    </ul>
  </div>
  </div>
</nav>

<div class="backround" style="background-image: url('../imgs/loginPlants.jpg');">
  <section class="container">
    <h3 class="adopt">These are your posts</h3>
    <section class="flex-row justify-center">
      {{!-- {{#each post}} --}}
      {{#each ownPlants as |post|}}
      <p>{{> displayOwn}}</p>
      {{/each}}
    </section>
  </section>
</div>