<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item">
        {{!-- look at href newUser that is how to set up the paths ... they will not be the names of the htmls but the
        will be the paths set up in homeRoutes that call diferent handlebars at this level will need ./ note that it is
        one dot here before the / not 2 dots--}}
        <a class="btn btn-outline" onclick="window.location" aria-current="page" href="./newUser">create a
          profile</a>
      </li>
    </ul>
  </div>
  </div>
</nav>
<h1>community garden - login</h1>
{{!-- the ../ will be needed to link imgs in the folder the way they are set up --}}
<div style="background-image: url('../imgs/loginPlants.jpg');">
  <div class="col row-6">

    <div class="col-4">
      <div class="row">
        {{!-- added class to form div --}}
        <form class="login-form">
          <div class="mb-3">
            <label for="exampleInputEmail1" class="form-label">email</label>
            <input type="email" class="form-control" id="email-login" aria-describedby="emailHelp">
          </div>
          <div class="mb-3">
            <label for="exampleInputPassword1" class="form-label">password</label>
            <input type="password" class="form-control" id="password-login">
          </div>

          <button class="loginbtn"> <a class="btn btn-outline" onclick="loginFormHandler()" aria-current="page"
              id="submit">submit</a></button>
        </form>
      </div>

    </div>

  </div>
  <div class="push"></div>

  <script src="/js/login.js"></script>